# Progress: asciiart-per-task

## Goal

In case the config asked to stop after every task - after the task is done, include an ASCIIART diagram of what the architecture/state is now and where we are going in the next task.

## Goal Context

Interview responses from goal clarification:
- Problem: Adding new functionality - add ASCII art diagrams that don't exist yet
- Constraints: Must integrate with existing plugin architecture
- Success criteria: Visual ASCII diagram appears after each task when reviewEachTask=true

## Completed Tasks

- [x] 1.1 Add diagram generation instructions to reviewEachTask pause block
- [x] 1.2 Update pause message template to include diagram placeholder
- [x] 1.3 Add edge case handling instructions

## Progress Log

- Created spec directory and initialized state
- Completed research phase
- Completed design phase
- Task 1.1: Added Generate Progress Diagram subsection to implement.md section 8

## Learnings

- reviewEachTask pause happens in implement.md coordinator, section 8 "State Update" (lines 423-460)
- Pause output location: after state update, before STOP - current message is 6 lines
- Full context available at pause: taskIndex, totalTasks, current/next task blocks, progress history, design.md
- design.md often contains Architecture section with existing ASCII diagrams - can be leveraged
- ASCII diagram characters: use `+`, `-`, `|`, `>` for maximum terminal compatibility
- No external tools needed - Claude can generate diagrams inline from context
- Integration point: modify pause message output to include diagram after task info
- Related spec: config-questions implements reviewEachTask - this feature uses its output
- Plugin is markdown-only - no build/test infrastructure, verification via grep patterns
- Requirements: defined two diagram formats - standard (phase boxes) and simplified (linear) for different spec sizes
- Edge cases identified: first task, last task, single-task phases, single-task specs
- Scope decision: exclude design.md architecture diagrams for now, keep minimal scope
- Key constraint: 70 char max width for terminal readability
- Design: single file modification to implement.md section 8 (coordinator pause)
- Design: horizontal phase columns (current + next) with collapsed distant phases
- Design: task labels truncated to 12 chars for consistent column width
- Design: edge cases require special messaging (first/last/single task scenarios)
- Design: verification via grep patterns + manual test with reviewEachTask=true spec
- Task planning: 11 total tasks across 4 phases (5 POC, 2 refactor, 0 test, 4 quality gates)
- Task planning: Phase 3 (Testing) skipped per interview - markdown-only changes
- Task planning: All tasks target single file: plugins/ralph-specum/commands/implement.md (except version bumps)
- Task planning: Verification via grep patterns since no build/lint/test infrastructure exists
- Task planning: Version bumps required in 2 files - plugin.json and marketplace.json
- Step numbering needed update after inserting new step (original step 2 became step 3, etc.)
- Task 1.2: Pause message template is output in step 3 of reviewEachTask block (lines ~488-500)
- Task 1.3: Edge cases added as subsection "e." in diagram generation constraints

## Current Task
Awaiting next task

## Next
Task 1.4: Add next task files extraction instructions
